# EnvWeave 0.1.0-alpha.1

这是 EnvWeave 的首个开发者预览版，重点验证 Linux/macOS 配置收集、Git 同步、可视化差异编辑、软件清单和事务恢复闭环。

安装前请阅读 [发布、安装与校验](../06-release.md) 和根目录 `CHANGELOG.md`。Alpha 版本必须配合独立系统备份使用。

本版本提供 Linux x86_64 AppImage/Debian 包，以及 macOS 12+ 的 Apple Silicon/Intel DMG。GitHub Release 默认生成为草稿预发布，维护者完成产物和签名审查后才可公开。
